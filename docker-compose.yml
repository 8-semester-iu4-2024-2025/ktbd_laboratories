services:
  oracledb:
    build:
      context: .
      dockerfile: oracle/Dockerfile
    ports:
      - "1521:1521"
  php:
    build:
      context: .
      dockerfile: php/Dockerfile
    ports:
      - "80:80"
    volumes:
      - ./labs:/var/www/html
